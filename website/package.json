{
  "name": "apelles-website",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Apelles's marketing + docs website (Astro). Deliberately NOT a member of the repo's npm workspaces — see website/README.md, D-255 and D-264.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run check && npm run build && npm run test"
  },
  "dependencies": {
    "astro": "5.16.2"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.4",
    "linkedom": "0.18.12",
    "typescript": "5.9.3",
    "vitest": "3.2.4"
  }
}
