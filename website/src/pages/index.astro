---
/**
 * index.astro — the home page (D-264).
 *
 * The section order is an argument: the promise (hero) → who it is for and
 * who it is not (difference) → what is in it (rooms) → why it is built this
 * way (machine) → the one ask (beta).
 *
 * D-279: Demo.astro (the scrubbable filmstrip of real screenshots) is not
 * rendered for now — the owner's own call, on the same honesty grounds this
 * site already applies to the Motion/Colorist rooms: the captures it was
 * built around predate the rename and are being retaken (TODO-RECAPTURE-
 * SHOTS.md), and a demo built on stand-in shots is a mockup by another name.
 * The component itself is untouched, not deleted — re-add the import and the
 * `<Demo />` line once real captures exist.
 */
import Base from '../layouts/Base.astro';
import Hero from '../components/Hero.astro';
import Difference from '../components/Difference.astro';
import Rooms from '../components/Rooms.astro';
import Machine from '../components/Machine.astro';
import Beta from '../components/Beta.astro';
---

<Base
  title="Apelles — the video app for people who never learned to edit"
  description="You already know what your video should look like. Apelles is the part you were missing: describe what you want, and a real AI agent does the cutting, the colour and the motion graphics — free, open source, and running on your own machine."
>
  <Hero />
  <Difference />
  <Rooms />
  <Machine />
  <Beta />
</Base>
