{
  "name": "@chroma/bridge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "Chroma typed Tauri command bindings + zustand stores + the control-bridge hook (D-020). First real content: the D-044 media-pool store.",
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "zustand": "^5.0.15"
  }
}
