# @chroma/shell

**The app shell** (D-039) — tab switcher (Edit / Motion / Colorist), project
launcher (D-037), window chrome, routing. Composes the tab packages; mounted by
`apps/desktop` (the Vite entry `src-tauri` serves).

**Status:** D-039 migration step 1 stub — placeholder export only.
