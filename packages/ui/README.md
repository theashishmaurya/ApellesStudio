# @chroma/ui

**Shared component kit** (D-039) — buttons, sliders, dropdowns, modals, panels,
adapted from RapidRAW's `app/src/components/ui/`. Depends on `@chroma/tokens`.

Consumed by `@chroma/colorist`, `@chroma/editor`, `@chroma/motion`,
`@chroma/shell`.

**Status:** D-039 migration step 1 stub — placeholder export only.
