/**
 * @chroma/motion — the Motion tab placeholder (D-039).
 *
 * A real Remotion player embed + manifest editor lands in a later D-039 step.
 */

export function MotionTab() {
  return (
    <div className="h-full w-full flex flex-col items-center justify-center gap-2 bg-bg-primary text-center px-6">
      <h1 className="text-lg font-semibold text-text-primary">Motion — coming soon</h1>
      <p className="text-sm text-text-secondary max-w-md">
        Remotion motion-graphics engine — <code>@chroma/motion-engine</code>.
      </p>
    </div>
  );
}
