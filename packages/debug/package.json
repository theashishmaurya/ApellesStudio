{
  "name": "@apelles/debug",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "Apelles internal debug tooling (D-219) — the debug_* control-server op registry that drives and inspects the RUNNING app: UI state (tab / Sources / Inspector), a bounded DOM-tree dump, and the Edit-tab preview's real frame timing. Debug-only: the whole registry is behind import.meta.env.DEV and is dropped from a production build.",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@apelles/editor": "*",
    "@apelles/shell": "*",
    "@tauri-apps/api": "^2.11.1",
    "react": "^19.2.8"
  },
  "devDependencies": {
    "@tauri-apps/plugin-dialog": "^2.7.2",
    "jsdom": "^29.1.1",
    "react-dom": "^19.2.8",
    "vitest": "^3.2.4"
  }
}
