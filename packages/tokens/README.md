# @chroma/tokens

**Design tokens + theme** for the Chroma frontend workspace (D-039). Colour,
spacing, radius, typography tokens and the light/dark theme — to be extracted
from RapidRAW's CSS variables in `app/src/`.

Consumed by `@chroma/ui` and every tab package.

**Status:** D-039 migration step 1 stub — `src/index.ts` placeholder export
only, not yet imported anywhere.
