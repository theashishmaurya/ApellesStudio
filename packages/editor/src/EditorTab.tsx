/**
 * @chroma/editor — the Editing tab placeholder (D-039).
 *
 * A real timeline / transcript-cut / multi-track UI lands in a later D-039 step.
 */

export function EditorTab() {
  return (
    <div className="h-full w-full flex flex-col items-center justify-center gap-2 bg-bg-primary text-center px-6">
      <h1 className="text-lg font-semibold text-text-primary">Editing — coming soon</h1>
      <p className="text-sm text-text-secondary max-w-md">
        Timeline, transcript cut, multi-track — D-039.
      </p>
    </div>
  );
}
