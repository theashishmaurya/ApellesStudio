[package]
name = "chroma-grade-model"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Chroma L2 — the grade.json document model (D-025): adjustments, mask geometry, keyframes, matte/track/depth refs. Pure model (vs chroma-grade, the renderer)."

[dependencies]
chroma-types = { path = "../chroma-types" }
serde = { workspace = true }
serde_json = { workspace = true }
