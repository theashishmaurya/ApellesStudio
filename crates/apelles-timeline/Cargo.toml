[package]
name = "apelles-timeline"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Apelles L2 — the OTIO-shaped edit model: tracks / clips, reorder / trim / split / remove, from_shots builder. Pure data + logic, no gpu/media."

[dependencies]
apelles-types = { path = "../apelles-types" }
serde = { workspace = true }
# D-082: `Clip::chroma_keyframes` is an opaque `serde_json::Value` (see that
# field's doc) — real dependency now, not just a test one.
serde_json = { workspace = true }
thiserror = { workspace = true }
