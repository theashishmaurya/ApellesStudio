[package]
name = "chroma-ai"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Chroma L2 — the AI sidecar client (D-145, crate-extraction-plan.md §2.5): lifecycle supervision (spawn/health/hash) for the ai/ FastAPI sidecar, plus the /segment, /track, /refine_track, /depth_track, /health HTTP wire plumbing. No AppHandle, no fork-side global state, no GPU."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
log = "0.4"
sha2 = "0.11"
image = "0.25.10"
reqwest = { version = "0.13", default-features = false, features = ["json", "rustls"] }
