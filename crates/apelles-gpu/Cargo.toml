[package]
name = "apelles-gpu"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Apelles L0 — headless wgpu device/queue/limits context (D-014/D-144), extracted from app/src-tauri's render_core.rs. No display surface, no grade renderer — those stay app-side pending apelles-grade."

[dependencies]
wgpu = "29.0" # Downgraded to prevent P3 color shifts on Apple devices — pinned to match app/src-tauri
pollster = "1.0"
log = "0.4"
