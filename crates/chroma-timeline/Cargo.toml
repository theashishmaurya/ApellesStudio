[package]
name = "chroma-timeline"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Chroma L2 — the OTIO-shaped edit model: tracks / clips, reorder / trim / split / remove, from_shots builder. Pure data + logic, no gpu/media."

[dependencies]
chroma-types = { path = "../chroma-types" }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
