/**
 * Re-export shim (D-039). The real component lives in `@chroma/ui`; this file
 * keeps the existing `import Input from '../ui/Input'` sites working.
 */
export { Input as default } from '@chroma/ui';
