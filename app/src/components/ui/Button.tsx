/**
 * Re-export shim (D-039). The real component lives in `@chroma/ui`; this file
 * keeps the existing `import Button from '../ui/Button'` sites working.
 */
export { Button as default } from '@chroma/ui';
